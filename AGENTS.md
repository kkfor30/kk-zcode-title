# 开发约定

文档统一使用中文。修改前先明确用户的真实任务与外层项目上下文。

每次修复都要检查导致错误的规则或流程，把可复现案例加入测试。不要只手工修复一个标题。

标题默认省略外层已有的项目名，只有 monorepo 子项目、上级目录内的具体项目或跨项目歧义需要名称。内容模型、公司或视频主题不是可以随意删除的项目前缀。

ZCode 没有官方改名接口；标题只能通过 `session` 表的标题字段短事务写入并读回核验，不得改写消息、权限或其他会话状态，不向话题发送改名指令。模型提示词只接收摘录数据。不得把真实对话、API key、批量处理清单或用户目录发布到仓库。

命名模型是插件直连的 OpenAI 兼容接口，与 ZCode 客户端模型配置互不影响；普通测试不得调用外部模型，只能用离线 fixture。

Hook 入口与 Worker 是两个进程：入口只解析事件并派生，Worker 走完整状态机；两者改动都要分别冒烟（`hook` 子命令返回空 JSON 且秒回，Worker 结果落在审计日志）。
